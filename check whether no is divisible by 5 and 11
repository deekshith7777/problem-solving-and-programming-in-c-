# include<stdio.h>
int main()
{
    int number;
    printf("enter any number");
    scanf("%d",&number);
    if((number%5==0) && (number%11==0))
    {
        printf("the number is divisible by 5 and 11");
    }
    else
    {
        printf("the number is not divislble by 5 and 11");
    }
    return 0;
}
